header
  img.logo(src="https://github.com/vimcar/frontend-challenge/raw/master/design/vimcar-logo.svg" alt="Vimcar Logo")
  nav.nav
    ul.nav__list
      for link in navigation
        if link.active
          li.nav__item.nav__item--active
            a.nav__link(href=link.url)= link.name
        else
          li.nav__item
            a.nav__link(href=link.url)= link.name
main!= content

